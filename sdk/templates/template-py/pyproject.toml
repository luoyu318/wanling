[project]
name = "wanling-agent-template-py"
version = "0.1.0"
license = "Apache-2.0"
requires-python = ">=3.10"
dependencies = ["wanling-sdk>=0.1"]

[tool.uv.sources]
wanling-sdk = { path = "../../python" }

[tool.uv]
package = false

[dependency-groups]
dev = ["ruff>=0.9"]
